<?xml version="1.0" encoding="utf-8" ?>
<xct:Popup
    x:Class="Pantry.pages.AddProductPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pantry="clr-namespace:Pantry"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    Size="300,400"
    Color="Transparent">
    <Frame CornerRadius="20" HasShadow="True">
        <StackLayout>
            <Label
                FontSize="Large"
                HorizontalTextAlignment="Center"
                Text="Add item to your product list" />
            <Entry
                x:Name="ProductName"
                Margin="10"
                Placeholder="Enter the product" />
            <Label
                x:Name="lblCorrect"
                HorizontalOptions="Center"
                IsVisible="false"
                Text="Enter only letters/"
                TextColor="Red" />
            <DatePicker x:Name="ExpiryDate" Margin="10" />
            <Picker Title="ProductType" Margin="10" />
            <Button
                Margin="10"
                Clicked="BtnProductAddToList"
                Text="Add Product" />
        </StackLayout>
    </Frame>
</xct:Popup>

