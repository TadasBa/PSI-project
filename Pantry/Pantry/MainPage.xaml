<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:converters="clr-namespace:Pantry.converters"
             x:Class="Pantry.MainPage">

    <ContentPage.Resources>
        <converters:DateTimeToString x:Key="formatDate"/>
    </ContentPage.Resources>
    
    <StackLayout>
        <SearchBar Placeholder="Iveskite produkta kurio ieskote" TextChanged="SearchBar_TextChanged"/>
        <ListView x:Name="itemListView" SelectedItem="{Binding selectedProduct}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding productName}"
                               Detail="{Binding expiryDate, Converter={StaticResource formatDate}}"
                               TextColor="#FF11FF"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Entry x:Name="ProductName" Placeholder="Iveskite produkta"/>
        <DatePicker x:Name="ExpiryDate" Format="MM/dd/yyyy"/>
        <Button Text="Add Product" Clicked="BtnProductAdd"/>
        <Button Text="Delete Product" Clicked="BtnProductDelete"/> 
    </StackLayout>

</ContentPage>
